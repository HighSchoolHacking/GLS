file start : Conversions Converters
    standalone functions declare start : export Converters
        standalone function declare start : public TryAsDouble void doubleLike string
            if string to double start : { variable : doubleLike } { variable : asDouble }
                print : { string format : ("It is a double: {0}") { math as int : { variable : asDouble } } int }
            if string to double end
        standalone function declare end

        standalone function declare start : public TryAsInt void firstIntLike string secondIntLike string
            if string to int start : { variable : firstIntLike } { variable : firstInt } { variable : secondIntLike } { variable : secondInt }
                print : { string format : ("Both are ints: {0} {1}") { variable : firstInt } int { variable : secondInt } int }
            if string to double end
        standalone function declare end
    standalone functions declare end
file end
