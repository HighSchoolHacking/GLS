file start : Dictionaries Main
    main context start
        main start
            comment line : Types
            variable declare : foo { dictionary type : string int } { dictionary new : string int }
            variable declare : bar { dictionary type : string { dictionary type : string int } } { dictionary new : string { dictionary type : string int } }

            comment line : Indices
            dictionary set : { variable : foo } "baz" 7
            variable declare : qux int { dictionary get : { variable : foo } "baz" }
            print : { string format : ("baz is {0}") { dictionary get : { variable : foo } "baz" } int }
            print : { string format : ("qux is {0}") { variable : qux } int }

            comment line : Initialization
            variable declare start : container { dictionary type : string int } { dictionary new start : string int }
                dictionary pair : "bbb" 1 ,
                dictionary pair : "ccc" 2 ,
                dictionary pair : "ddd" 3
            dictionary new end

            comment line : Contains Key
            variable declare : containsFalse boolean { dictionary contains key : { variable : container } "aaa" }

            if start : { variable : containsFalse }
                print : "wrong"
            if end

            if start : { dictionary contains key : { variable : container } "bbb" }
                print : ("contains bbb")
            if end

            comment line : Setting
            dictionary set : { variable : container } "aaa" 7
            print : { dictionary get : { variable : container } "aaa" }
        main end
    main context end
file end
