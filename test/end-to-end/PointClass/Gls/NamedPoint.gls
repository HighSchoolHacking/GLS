file start : PointClass NamedPoint
    import local : PointClass Point use Point

    class start : export NamedPoint extends Point
        member variable declare : private Name string

        constructor start : public NamedPoint x int y int name string base x y
            operation : { member variable : private { this } Name } equals name
        constructor end

        member function declare start : public GetLabel string
            return : { string format : ("{0} at [{1}, {2}] with vector {3}") { member variable : private { this } Name } string { member variable : public { this } X } int { member variable : public { this } Y } int { member variable : protected { this } Vector } int }
        member function declare end
    class end
file end
